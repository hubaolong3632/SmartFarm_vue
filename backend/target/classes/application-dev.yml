# 本地环境
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/greenhouse_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # MyBatis-Plus配置
  sql:
    init:
      mode: never  # 不自动执行初始化脚本

  # Jackson配置
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
    default-property-inclusion: non_null  # 不序列化 null 值

# 服务器环境
#spring:
#  # 数据源配置
#  datasource:
#    url: jdbc:mysql://mg_mysql.00000.work:3306/greenhouse_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
#    username: greenhouse_db
#    password: iYDrDXFMThm5yks8
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    hikari:
#      maximum-pool-size: 20
#      minimum-idle: 5
#      connection-timeout: 30000
#      idle-timeout: 600000
#      max-lifetime: 1800000
#
#  # MyBatis-Plus配置
#  sql:
#    init:
#      mode: never  # 不自动执行初始化脚本
#
#  # Jackson配置
#  jackson:
#    time-zone: Asia/Shanghai
#    date-format: yyyy-MM-dd HH:mm:ss
#    default-property-inclusion: always  # 确保所有字段都序列化（包括 null 值）
