# AI自动托管功能使用说明

## 功能概述

AI自动托管功能是一个智能化的温室管理系统，可以自动监控传感器数据，并根据预设的阈值自动执行控制操作，包括：
- 自动控制水的开关（抽水）
- 自动控制补光灯
- 自动执行土壤配方
- 通过邮箱发送报警通知

## 功能特点

1. **定时自动执行**：每10分钟自动检查一次传感器数据并执行相应的控制操作
2. **可配置开关**：可以单独启用/禁用整个功能或各个子功能
3. **邮件报警**：当检测到问题时，自动发送邮件通知
4. **执行日志**：记录每次执行的详细日志，包括执行的操作和检测到的问题

## 数据库设置

首先需要执行数据库脚本创建相关表：

```sql
-- 执行 backend/database/ai_hosting_tables.sql
```

## 后端配置

### 1. 邮件服务配置

在 `backend/src/main/resources/application-dev.yml` 中配置邮件服务：

```yaml
spring:
  mail:
    enabled: true  # 是否启用邮件服务
    host: smtp.qq.com  # SMTP服务器地址（QQ邮箱示例）
    port: 587  # SMTP端口
    username: your-email@qq.com  # 发件人邮箱
    password: your-email-password  # 邮箱授权码（不是登录密码）
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
```

**注意**：
- 如果使用QQ邮箱，需要在QQ邮箱设置中开启SMTP服务并获取授权码
- 如果使用其他邮箱服务商，请修改相应的host和port配置

### 2. 依赖检查

确保 `pom.xml` 中已添加邮件依赖：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-mail</artifactId>
</dependency>
```

## 前端使用

### 1. 访问AI托管管理页面

在系统导航菜单中点击"AI自动托管"进入管理页面。

### 2. 配置AI托管

在配置区域可以设置：

- **启用AI自动托管**：总开关，控制是否启用整个功能
- **邮件报警设置**：
  - 启用邮件报警：是否发送邮件通知
  - 接收邮箱地址：接收报警邮件的邮箱
- **控制功能**：
  - 自动控制水：是否自动控制抽水
  - 自动控制补光：是否自动控制补光灯
  - 自动执行土壤配方：是否自动执行配方
- **检查间隔**：执行检查的间隔时间（分钟，当前固定为10分钟）

### 3. 查看执行日志

在右侧日志区域可以查看：
- 执行状态（成功/失败/部分成功）
- 执行时间
- 执行的操作列表
- 检测到的问题列表
- 是否已发送邮件

### 4. 查看统计信息

页面顶部显示统计信息：
- 总执行次数
- 成功次数
- 失败次数
- 成功率

## 工作原理

1. **定时触发**：系统每10分钟自动触发一次AI托管任务
2. **数据检查**：获取最新的传感器数据
3. **阈值对比**：与自动化设置中的阈值进行对比
4. **执行操作**：
   - 如果光照低于阈值，自动开启补光灯
   - 如果土壤湿度低于阈值，自动启动抽水
   - 如果土壤湿度较低，自动执行相应的土壤配方
5. **问题检测**：检查温度、湿度、氧气、二氧化碳等指标是否异常
6. **邮件报警**：如果检测到问题或执行了操作，发送邮件通知
7. **日志记录**：记录所有执行详情到数据库

## 控制逻辑

### 补光控制
- 当光照强度低于设定的阈值时，自动开启补光灯
- 当光照强度达到或超过阈值时，自动关闭补光灯

### 抽水控制
- 当土壤湿度低于设定的阈值时，自动启动抽水

### 配方执行
- 当土壤湿度低于40%时，自动执行分配给各地块的配方

### 问题检测
- 温度过高/过低：超出设定的温度阈值范围
- 湿度过高/过低：超出设定的湿度阈值范围
- 氧气含量过低：低于设定的氧气阈值
- 二氧化碳含量过高：高于设定的二氧化碳阈值

## 注意事项

1. **邮件配置**：确保邮件服务配置正确，否则无法发送报警邮件
2. **自动化设置**：AI托管功能依赖于自动化设置中的阈值配置
3. **配方分配**：自动执行配方功能需要先在地块中分配配方
4. **MQTT连接**：控制操作通过MQTT发送，确保MQTT服务正常运行
5. **执行间隔**：当前固定为10分钟执行一次，无法动态调整

## 故障排查

### 邮件发送失败
- 检查邮件配置是否正确
- 确认邮箱授权码是否正确（不是登录密码）
- 检查SMTP服务器地址和端口是否正确
- 查看后端日志中的错误信息

### 控制操作未执行
- 确认AI托管功能已启用
- 检查相应的子功能开关是否开启
- 查看执行日志了解具体原因
- 确认MQTT服务连接正常

### 未检测到传感器数据
- 确认传感器数据正常采集
- 检查数据库中的传感器数据表是否有最新数据

## API接口

### 获取配置
```
GET /api/ai-hosting/config
```

### 更新配置
```
PUT /api/ai-hosting/config
Body: {
  enabled: boolean,
  emailEnabled: boolean,
  emailAddress: string,
  checkIntervalMinutes: number,
  waterControlEnabled: boolean,
  lightControlEnabled: boolean,
  recipeExecutionEnabled: boolean
}
```

### 获取执行日志
```
GET /api/ai-hosting/logs?limit=50
```

### 获取统计信息
```
GET /api/ai-hosting/stats
```

## 总结

AI自动托管功能为温室管理提供了智能化的自动化控制能力，可以大大减少人工干预，提高管理效率。通过合理配置和使用，可以实现温室环境的自动优化和问题预警。

